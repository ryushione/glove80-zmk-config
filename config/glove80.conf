# Studio enable + studio-rpc-usb-uart snippet are applied via build.yaml's
# matrix entry for glove80_lh, so this file stays empty (matching the
# pattern used by IevgeniiB/zmk-config and other working Studio configs).
# Add Glove80-specific Kconfig overrides here if you need them later.
